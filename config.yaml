# ============================================================
# ClipCast Studio — API Credentials
# ============================================================
# IMPORTANT: Never commit this file to git or share it publicly.
# Add "config.yaml" to your .gitignore file.
#
# All preferences (streamers, templates, posting times, etc.)
# live in preferences.yaml — this file is for secrets ONLY.
# ============================================================

# ── Twitch ──────────────────────────────────────────────────
# Get these at: https://dev.twitch.tv/console
twitch:
  client_id: "x68kwa8eqf6uelcnj2668rfcv3ba93"
  client_secret: "k8blan4wad1ggio8gthicw46bi4i17"

# ── YouTube ─────────────────────────────────────────────────
# Get this at: https://console.cloud.google.com/
# Enable "YouTube Data API v3" in your project
youtube:
  api_key: "AIzaSyCFQBbnszTEl71nnz8kGY_Cegpx2vqNmi8"

# ── TikTok ──────────────────────────────────────────────────
# Get these at: https://developers.tiktok.com/
# You need to create an app and request Content Posting API access
tiktok:
  client_key: "YOUR_TIKTOK_CLIENT_KEY_HERE"
  client_secret: "YOUR_TIKTOK_CLIENT_SECRET_HERE"
  # OAuth tokens — these are filled in automatically after you run the
  # TikTok auth flow. Do not edit these manually.
  access_token: ""
  refresh_token: ""
  token_expires_at: ""  # ISO 8601 datetime, e.g. "2025-01-01T00:00:00"
